# スマートコントラクトの開発
ローカルに独自開発したアバランチのチェーンでデプロイするスマートコントラクト

***
## コマンド集

プロジェクトの初期化（Hardhat）
```
npm init -y
npm install --save-dev hardhat @openzeppelin/test-helpers
npm install dotenv 
```

プロジェクトの新規作成
```
npx hardhat
```